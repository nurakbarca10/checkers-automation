{
  "name": "checkers",
  "version": "1.0.0",
  "description": "A Cypress-powered test framework for validating the Checkers game",
  "scripts": {
    "lint": "eslint cypress/**/*.js",
    "lint:fix": "eslint cypress/**/*.js --fix",
    "test": "npx cypress run",
    "test:open": "npx cypress open",
    "test:report": "npx cypress run --reporter mochawesome",
    "test:report:open": "npx cypress run && open cypress/reports/html/index.html",
    "test:report:open:chrome": "npx cypress run --browser chrome && open cypress/reports/html/index.html",
    "test:report:open:chrome:headed": "npx cypress run --browser chrome --headed && open cypress/reports/html/index.html"
  },
  "author": "Nurak Imanaliev",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.15.2"
  },
  "devDependencies": {
    "cypress-iframe": "^1.0.1",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-plugin-xhr-toggle": "^1.2.1",
    "cypress-real-events": "^1.13.0",
    "eslint-plugin-cypress": "^4.1.0"
  }
}
